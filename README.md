# Webinar-OS · וובינר חי שלם, ב-10 שלבים, עד הדף באוויר

הפקת וובינר היא 15 משימות קטנות על 8 כלים: מחקר, מצגת, תסריט, דף הרשמה, דף תודה, קבוצה, מיילים, וואטסאפים, זום. כל אחת "קטנה", וביחד זה שבוע, ותמיד שוכחים משהו.

**Webinar-OS** הוא סקיל ל-Claude Code. כותבים `/webinar-os`, עונים על 8 שאלות תוכן (ועוד 3 קצרות על הטון; בשלבים 2 ו-3 יש עוד שאלת בחירה קצרה אחת בכל שלב: מצגת HTML או מתווה, ונרשמים לגיליון או כפתור לקבוצה), ו-Claude מוביל/ה אותך ב-10 שלבים קבועים, באותו סדר, כל שלב מסתיים בקבצים מוכנים ובהוראה ידנית של דקה-שתיים. שלושה אישורים בדרך (קופי הדף, התזכורות, העיצוב). **הזרימה נגמרת בכתובת חיה**: בשלב 9 הדף עולה לאוויר, ואת/ה מקבל/ת לינק שאפשר להדביק בפרסום. כל הריצה: כ-75-110 דקות, מתוכן 3-5 דקות שקטות במחקר של שלב 1 (ב-Windows עד 5) ו-8-12 דקות שקטות בשלב 2. זה תקין, ככה זה נמדד. אם עברו 15 דקות בלי כלום, זה הזמן לבדוק. בזמן הריצה Claude יבקש אישור לפני כל הרצת סקריפט או פתיחת קובץ; לוחצים Yes (או "Yes, don't ask again" לפקודה הזו). בלי מפתחות API ובלי חיבורים. שאלות העיצוב נשאלות רק בשלב 8, אחרי שכבר יש מה לראות. עובד אצל כולם, ב-Mac וב-Windows, בחינם.

## מה מקבלים (9 השלבים)

| # | שלב | מה יוצא |
|---|---|---|
| 1 | מחקר | 8 תשובות + 6-10 עובדות מאומתות עם מקור ולינק → `research.md` |
| 2 | מצגת ותסריט | מצגת HTML (14 מערכות, כ-30-40 שקפים, PDF דרך הדפדפן ב-2 צעדים) + תסריט מתוזמן 2.5 מילים לשנייה → `deck.html`, `script.md` (או מתווה ל-Canva / PowerPoint) |
| 3 | דף נחיתה | דף הרשמה ב-8 סקשנים, נייד-קודם, ספירה לאחור, טופס עם תיבת הסכמה אחת (הנרשמים לגיליון Google) או כפתור לקבוצה → `landing/index.html` |
| 4 | דף תודה | "נשאר צעד אחד: הקבוצה", 3 צעדים, התאריך → `landing/thank-you/index.html` |
| 5 | קבוצת וואטסאפ | שם, הגדרות קבוצה שקטה, קבוצה 2 ב-1,024, הודעת פתיחה → `whatsapp-group.md` |
| 6 | תזכורות במייל ובוואטסאפ | 3 מיילים + 4 הודעות וואטסאפ + לוח שילוח עם תאריכים ושעות מדויקים, מוכנים להדבקה בכל מערכת → `emails.md`, `whatsapp.md`, `schedule.md` |
| 7 | הוובינר | הקמת וובינר בזום (לא פגישה), הודעת "התחלנו", צ'קליסט 10 דקות, ומה סופרים אחרי → `webinar-day.md`, `after.md` |
| 8 | עיצוב מותאם | 5 שאלות אחרי שראית תוצרים אמיתיים, 3 כיוונים למצגת ו-3 לדף, כל אחד מרונדר לתצוגה אמיתית שאפשר לפתוח, בוחר/ת, ואני מחיל/ה → `deck.html` ו-`landing/` בעיצוב שלך |
| 9 | העלאה לאוויר | הדף עולה לכתובת חיה (Netlify דרך Claude, גרירה ידנית, או אתר משלך), בדיקות על הכתובת החיה, ואיפה הלידים נשמרים → כתובת חיה + `/thank-you/` |

הכל נשמר ב-`outputs/webinars/<slug>/` בפרויקט שלך. עד שלב 8 המצגת והדפים חולקים מערכת עיצוב אחת (כהה, פונט Heebo, צבע הדגשה חם אחד), אז זה נראה מקצועי בלי מעצב/ת מהרגע הראשון; בשלב 8 מחליפים אותה בכיוון שבחרת, והכל מתעדכן ביחד. נעצר/ת באמצע? `/webinar-os המשך` ממשיך מהשלב האחרון שהושלם.

## התקנה ב-3 צעדים

**לפני שמתחילים:** צריך Claude Code מותקן ומחובר לחשבון. אין? נכנסים ל-claude.com/code, מתקינים לפי ההוראות שם, פותחים טרמינל חדש ומריצים `claude` פעם אחת כדי להתחבר. רק אחרי שזה עובד ממשיכים לצעד 1.

1. יוצרים (או בוחרים) תיקיית פרויקט שבה תעבדו, למשל תיקייה ריקה בשם `webinars` בתוך Documents.
2. מורידים את הזיפ ופותחים אותו **בתוך התיקייה הזו**. מקבלים שם תיקייה בשם `webinar-os`. (חשוב: Claude Code קורא רק קבצים בתוך תיקיית הפרויקט; זיפ שנשאר ב"הורדות" הוא לא רואה.)
3. פותחים טרמינל (Mac: Terminal, Windows: PowerShell), מקלידים `cd ` ואז גוררים את תיקיית הפרויקט לחלון, Enter, ואז מקלידים `claude` ו-Enter. (הטרמינל עונה `claude: command not found` או `'claude' is not recognized`? Claude Code לא מותקן, או שהטרמינל נפתח לפני ההתקנה: ראו "לפני שמתחילים" למעלה, ואת `skills/webinar-os/reference/troubleshoot.md` שורה 0.) כש-Claude Code נפתח, מדביקים לו את הפרומפט הזה:

```
התקן/י את הסקיל webinar-os מהתיקייה webinar-os (בתוך הפרויקט) לפי הקובץ INSTALL.md שבתוכה. בסוף אמור/י לי שהכל תקין ומה הפקודה להפעלה.
```

לא בטוח/ה בנתיב? גורר/ת את התיקייה `webinar-os` לחלון הטרמינל והנתיב המלא יודבק לבד.

בדרך Claude Code יבקש כמה בקשות הרשאה (העתקה לתיקייה `.claude/`, הרצת Python, יצירת תיקייה): מאשרים כל אחת, זו הגנה של Claude Code, לא תקלה. מכאן Claude מעתיק/ה את הסקיל ל-`.claude/skills/webinar-os/` בפרויקט, מריץ/ה בדיקה של 5 שניות, ומחזיר/ה "מוכן". מאז: `/webinar-os` בשיחה חדשה, וזה מתחיל מיד בשאלה הראשונה.

## האם זה יעבוד אצלי?

| יש לי | מה קורה |
|---|---|
| בלי Claude Code בכלל | קודם מתקינים: claude.com/code (Mac או Windows), טרמינל חדש, `claude` פעם אחת להתחברות. בלי זה אין למי להדביק את הפרומפט |
| Claude Code על Mac | **כן.** Python 3.9+ כבר מותקן ברוב המחשבים |
| Claude Code על Windows | **כן.** צריך Python מ-python.org (לסמן "Add python.exe to PATH"). הפקודה `python` במקום `python3` |
| בלי אינטרנט בזמן הצפייה | הדפים והמצגת עובדים; רק הפונט נופל לפונט המערכת. העימוד זהה |
| בלי מערכת דיוור | המיילים יוצאים כטקסט מוכן להדבקה; שולחים מ-Gmail ב-BCC או מכל מערכת |
| בלי תוסף וובינרים בזום | הוראות לפגישה רגילה עם ההגדרות הנכונות |
| רוצה עיצוב משלי | זה שלב 8, בתוך הזרימה. לא צריך לעשות כלום מראש |
| רוצה שהדף יהיה באוויר | זה שלב 9. חשבון Netlify חינם, וטוקן שמדביקים פעם אחת (סיסמה: לא נשמר בשום קובץ) |
| רוצה חיבורים / קריאייטיבים לרשתות / תמונות בדף | אחרי הוובינר הראשון: `skills/webinar-os/reference/advanced.md` |
| Claude.ai / ChatGPT בלי Claude Code | best-effort בלבד, בלי אחריות: `skills/webinar-os/reference/other-tools.md` |
| קריאייטיבים וקופי למודעות | פוסטים לכל רשת (סטורי, פייסבוק, וואטסאפ, דיוור, מודעה) + תמונות, עם מפתח OpenAI. אין מפתח? הטקסטים עדיין נכתבים |

**מדיניות פרטיות:** הסקיל שואל על לינק. אין? הוא אומר שצריך אחד לפני שהדף עולה לאוויר, ובונה בכל מקרה. הוא לא מנסח מדיניות בעצמו.

## מה בתוך החבילה

```
webinar-os/
  README.md                       הקובץ הזה
  INSTALL.md                      מה Claude עושה בהתקנה (Mac + Windows)
  LICENSE                         MIT
  .claude-plugin/plugin.json      מטא-דאטה לפלאגין
  skills/webinar-os/
    SKILL.md                      הזרימה: 9 השלבים, המכניקה, הפלט
    config.template.json          מבנה הקונפיג (העובדות של הוובינר)
    reference/
      research.md                 שלב 1: השאלות, המחקר
      deck-structure.md           שלב 2: 14 המערכות, deck.json, התסריט
      copy-blocks.md              שלבים 3-4: 8 הסקשנים, copy.json, צ'קליסט
      manual-paths.md             Google Form, קבוצת וואטסאפ, פתיחה והעלאה של הדפים
      schedule.md                 שלב 6: מיילים, וואטסאפ, לוח שילוח
      webinar-day.md              שלב 7: זום, צ'קליסט, after.md
      design-round.md             שלב 8: 5 שאלות, 6 כיוונים מוכנים, תצוגות, שער AA
      publish.md                  שלב 9: 3 מסלולי העלאה, הטוקן, הבדיקות החיות, דומיין
      design-system.md            מערכת העיצוב (8 טוקנים, דקדוק אחד ל-4 קבצים)
      advanced.md                 מתקדם: תמונות, חיבורים, קריאייטיבים, מכירות
      troubleshoot.md             11 התקלות הצפויות
      other-tools.md              7 פרומפטים לצ'אט רגיל (בלי אחריות)
    templates/
      landing.html                דף הרשמה (8 סקשנים)
      thank-you.html              דף תודה
      consent-block.html          תיבת ההסכמה האחת
      deck-basic.html             מצגת (8 תבניות שקף)
      creative.html               קריאייטיב 1080×1080 (מתקדם)
    scripts/
      render_pages.py             JSON → דפים + מצגת + תסריט + תצוגות שלב 8 (ספרייה סטנדרטית)
      deploy_netlify.py           שלב 9: התיקייה landing עולה לאוויר בפקודה אחת
      doctor.py                   בדיקה של 5 שניות אחרי ההתקנה
      screenshot.py, dub_links.py, env_set.py   מתקדם
```

## כללים שהסקיל לא מוותר עליהם

- עברית, פנייה דו-מגדרית, בלי קו מפריד ארוך.
- אף נתון בלי מקור. אין חיפוש? הוא אומר את זה ומבקש עובדות ממך.
- 10 שלבים, סדר קבוע, "ממשיכים?" אחרי כל אחד. בדיוק 3 עצירות אישור.
- כפתור אחד בדף. טלפון חובה. תיבת הסכמה אחת, לא שתיים.
- משנים JSON ומרנדרים; לא עורכים HTML ביד.
- טוקן הוא סיסמה. הוא לא נשמר בקובץ, לא בקונפיג ולא בגיט.

---

נבנה עם Claude Code · משותף על ידי [Brain by Eden Bibas](https://brainai.co.il)
