---
name: webinar-os
description: Build a complete LIVE webinar step by step, in Hebrew, with zero room for error - 7 numbered steps in a fixed order (research with verified facts, deck + presenter script, landing page, thank-you page, WhatsApp group, email + WhatsApp reminders with a send schedule, Zoom setup + pre-live checklist + post-webinar count). Everything comes out as files plus 1-2 minute manual instructions that work for everyone (Mac and Windows, no API keys, no integrations). One unified dark design system across deck and pages (creatives in the advanced path). Resumable with "/webinar-os המשך". Use when the user says "/webinar-os", "תבנה לי וובינר", "בוא נבנה וובינר", "הפקת וובינר", "וובינר חי", "webinar", or wants to run a live online session with registration. Advanced (branding, integrations, creatives, sales layer) lives in reference/advanced.md and is never part of the base flow.
---

# /webinar-os · וובינר חי ב-7 שלבים

זרימה אחת, קבועה, לכולם. 7 שלבים ממוספרים, באותו סדר ובאותם שמות (הם תואמים 1:1 לסדנה, לא משנים). כל שלב מסתיים בקבצים בתיקייה + הוראה ידנית של דקה-שתיים, ורק אז ממשיכים. בלי מפתחות, בלי חיבורים, בלי שאלות עיצוב. מי שרוצה יותר: `reference/advanced.md` (מיתוג, תמונות, חיבורים, קריאייטיבים, שכבת מכירות), רק אם ביקשו.

## דוקטרינה (לא מתפשרים)

- **עברית, פנייה דו-מגדרית** (יחיד/ה קודם: "בעל/ת עסק", "תוכל/י"), בלי קו מפריד ארוך (רק מקף רגיל), בלי מילה לועזית בתחילת שורה.
- **לא ממציאים נתונים.** רק עובדות שאומתו ברשת עם מקור ולינק. אין חיפוש? אומרים את זה ומבקשים 3 עובדות מהמשתמש/ת.
- **בדיוק 2 עצירות אישור:** (1) קופי הדף בשלב 3, (2) התזכורות בשלב 6. כל השאר אוטומטי, אבל אחרי כל שלב מחכים ל"המשך".
- **מילון קבוע** (הסדנה, ה-README והמצגת משתמשים בו מילה במילה, לא משנים): **השלבים** = מחקר · מצגת ותסריט · דף נחיתה · דף תודה · קבוצת וואטסאפ · תזכורות במייל ובוואטסאפ · הוובינר. **מבנה המצגת** = "14 מערכות" (לא "אקטים"). **הפקודות** = `/webinar-os` (התחלה), `המשך` (אחרי כל שלב), `מאושר` (בשתי עצירות האישור). "מאושר" ו"המשך" הן המילים הקנוניות; כל אישור אחר ("כן", "יאללה") מתקבל גם.
- **מערכת עיצוב אחת**, כהה, ב-`reference/design-system.md`. לא שואלים על צבע, פונט או לוגו בבסיס.
- **תיבת הסכמה אחת** בכל טופס, עם לינק למדיניות פרטיות. אין מדיניות? אומרים בשורה אחת שצריך אחת לפני שעולים לאוויר, ומשאירים ריק. **הסקיל לא מנסח מדיניות פרטיות.**
- **כל ריצה חיה ב-`outputs/webinars/<slug>/`.** `config.json` = העובדות, `copy.json` = הקופי, `deck.json` = המצגת, `state.json` = איפה עצרנו. הקבצים שנוצרים (`landing/`, `deck.html`) לא נערכים ביד; משנים את ה-JSON ומרנדרים שוב.
- `<skill-dir>` = התיקייה של הקובץ הזה. הפקודה `python3`; ב-Windows `python`.

## מפת קבצי העיון (קוראים רק כשמגיעים לשלב)

| קובץ | מתי |
|---|---|
| `reference/research.md` | שלב 1: 8 השאלות, 3 שאלות הקול, פרוטוקול המחקר, פורמט `research.md` |
| `reference/deck-structure.md` | שלב 2: 14 המערכות, 8 תבניות השקף, `deck.json`, `script.md` |
| `reference/copy-blocks.md` | שלבים 3-4: 8 הסקשנים, `copy.json`, דף התודה, עצירת האישור, צ'קליסט |
| `reference/manual-paths.md` | שלבים 3-5: Google Form, קבוצת וואטסאפ, פתיחת הדפים, העלאה לאוויר, יומן |
| `reference/schedule.md` | שלב 6: 3 מיילים, 4 הודעות, לוח השילוח, עצירת האישור; §0 = הלוח המצומצם למצב כפתור |
| `reference/webinar-day.md` | שלב 7: זום, "התחלנו", צ'קליסט 10 דקות, `after.md` |
| `reference/design-system.md` | מערכת העיצוב (מידע; לא נוגעים בבסיס) |
| `reference/advanced.md` | רק לבקשה: מיתוג, תמונות, חיבורים, קריאייטיבים, שאלות נוספות, שכבת מכירות |
| `reference/troubleshoot.md` | כשמשהו נתקע |
| `reference/other-tools.md` | מי שבלי Claude Code (בלי אחריות) |

סקריפט אחד בבסיס: `scripts/render_pages.py` (ספרייה סטנדרטית, Python 3.9+, Mac/Windows). `scripts/doctor.py` = בדיקה של 5 שניות שהכל במקום. שאר הסקריפטים (`screenshot.py`, `deploy_netlify.py`, `dub_links.py`, `env_set.py`) שייכים למסלול המתקדם.

## מכניקת הזרימה

1. **התחלה:** `/webinar-os` = מתחילים מיד בשאלה הראשונה של שלב 1. בלי הקדמות, בלי "מה מחובר אצלך".
2. **אחרי כל שלב** מדפיסים בדיוק:
   `✅ שלב N הושלם: <שורה אחת> · קבצים: <שמות>. ממשיכים לשלב N+1?`
   ומחכים. "המשך" / "כן" / "יאללה" / כל אישור = ממשיכים. בקשת תיקון = מתקנים ושואלים שוב.
3. **מצב:** אחרי כל שלב כותבים `outputs/webinars/<slug>/state.json`:
   `{"slug": "...", "project_name": "...", "completed": [1, 2], "updated": "<ISO>"}`.
   `updated` = זמן אמיתי מהשעון, בכל שלב מ-1 עד 7, לא זמן מומצא ולא חצות עגולה. מריצים ומדביקים את הפלט. בכלי Bash (כולל Git Bash ב-Windows): `date +%Y-%m-%dT%H:%M:%S%z`; רק בכלי PowerShell: `Get-Date -Format o`. הפיצול לפי הכלי, לא לפי מערכת ההפעלה.
4. **המשך:** `/webinar-os המשך` = מחפשים `outputs/webinars/*/state.json`; אחד = טוענים אותו, קוראים את `config.json`, מדפיסים "ממשיכים את <project_name> משלב N" ומתחילים את השלב הבא. כמה = שואלים איזה. אין = מתחילים מ-1.
5. **לא מדלגים ולא מאחדים שלבים.** גם אם המשתמש/ת "יודע/ת מה הוא/היא רוצה", עוברים 1 → 7.

---

## שלב 1 · מחקר

קוראים `reference/research.md`.

1. **8 שאלות תוכן**, אחת-אחת (או 2-3 בסבב עם AskUserQuestion), עם ברירת מחדל לאישור: נושא · מה יוצאים איתו ביד · הקהל כאדם אחד · תאריך/שעה/אורך/פלטפורמה/הקלטה · מי מעביר/ה + הוכחה אחת · מה אחרי ההרשמה (ברירת מחדל: קבוצת וואטסאפ) · slug · לינק למדיניות פרטיות. **שאלה 6 קובעת מסלול:** "קבוצת וואטסאפ" = המסלול הרגיל. "מייל בלבד" או "יומן" = **מסלול מייל** (`next_step: email` / `calendar`): אין קבוצת וואטסאפ, `group_link` נשאר ריק, ושלבים 4-6 מסתעפים לפי זה (מסומן למטה). מסלול מייל מחייב מצב (א) בשלב 3, כי בלי כתובות מייל ובלי קבוצה אין למי לשלוח תזכורות; אומרים את זה כבר עכשיו.
2. **קול:** יש קובץ קול בפרויקט? קוראים. אין? 3 שאלות קצרות (איך מדברים / לשון פנייה / מילים אסורות).
3. **מחקר ברשת:** לפני החיפוש אומרים למשתמש/ת בשורה: "מחפש/ת ברשת, 3-5 דקות בלי פלט (ב-Windows עד 5). זה תקין. אם עברו 15 דקות בלי כלום, זה הזמן לבדוק." `WebSearch` + `WebFetch` של Claude Code. 6-10 עובדות, כל אחת עם מקור, שנה ולינק שנפתח ואומת. אין כלי חיפוש? אומרים את זה במשפט, ומבקשים 3 עובדות מהמשתמש/ת. לא ממציאים.
4. כותבים `config.json` (לפי `config.template.json`), `research.md` (בלי קו מפריד ארוך, גם לא בציטוטים מהמקורות: מחליפים למקף או לפסיק), `state.json` עם זמן אמיתי (בכלי Bash, כולל Git Bash ב-Windows: `date +%Y-%m-%dT%H:%M:%S%z`; רק בכלי PowerShell: `Get-Date -Format o`; לא זמן מומצא).

`✅ שלב 1 הושלם: 8 תשובות + N עובדות מאומתות · קבצים: config.json, research.md. ממשיכים לשלב 2?`

---

## שלב 2 · מצגת ותסריט

לפני שמתחילים אומרים למשתמש/ת בשורה: "השלב הזה הוא הארוך ביותר, 8-12 דקות בלי פלט. זה תקין. אם עברו 15 דקות בלי כלום, זה הזמן לבדוק." שקפי `screenshot` מקבלים שם קובץ מפורש ב-`src`, באותה תיקייה של `deck.html` (`screenshot-1.png`, `screenshot-2.png` וכך הלאה, כמו ב-`deck-structure.md`), ובסיום מציגים את רשימת השמות כדי שיהיה ברור מה לצלם ואיך לקרוא לקובץ.

קוראים `reference/deck-structure.md`.

1. שואלים שאלה אחת: "מצגת HTML מוכנה (ברירת מחדל), או מתווה להדבקה ב-Canva / PowerPoint / Google Slides?"
2. **HTML:** כותבים `deck.json` לפי 14 המערכות (כ-30-40 שקפים, 8 תבניות), עם `notes` לכל שקף. 3 עובדות מ-`research.md` = 3 שקפי `big-stat` עם מקור: בדיוק העובדות שרשומות שם תחת "3 עובדות שנכנסות למצגת", כל עובדה בשקף אחד בלבד, ו-`num`, `source`, `source_url` מועתקים אות באות משורת הטבלה (הלינק הראשוני והשנה של המקור, לא בלוג שמצטט אותו; מספר שאינו אחוז מותר לקצר לתצוגה, למשל `29+`, והמספר המלא נכתב ב-`claim`). **כלל המספרים:** כל מספר או אחוז שנאמר ב-`notes` חייב להופיע בטבלת `research.md` עם אותו מקור ואותו ערך בדיוק, גם כשהוא כתוב במילים (63.5% נאמר "שישים ושלושה וחצי אחוז", לא "שישים ושלושה" ולא "שני עסקים מכל שלושה"); מספר שלא בטבלה = לא נאמר, וטענה כללית עם מספר ("עובד חדש שוכח חצי מההדרכה") נמחקת או מקבלת שורה בטבלה. חשבון להמחשה מותר רק במשפט שנפתח ב-"נניח" או "לדוגמה בלבד" ובלי אחוזים בתוכו. חריגים: ביטויי זמן שגרתיים ("רבע שעה ביום", "חצי שנה") ומספר על העסק של המגיש/ה עצמו/ה שהוא/היא יכול/ה לאמת מהנתונים שלו/ה. בדיקה עצמית לפני הרינדור: grep ב-`notes` של `אחוז|אחוזים|אלף|אלפים|מאה|מאות|שקל|שקלים|חצי|רבע|שליש|פי [א-ת]|\d` (כמילים שלמות), וכל תוצאה חייבת להתאים לשורה ב-`research.md` עם הערך הזהה, או להיות חשבון-"נניח", או אחד החריגים; grep של כל `source_url` ב-`deck.json` מול עמודת הלינק ב-`research.md` (לינק שלא בטבלה = לא נכנס; `research.md` הוא עובדות בלבד, בלי הערות על מצב המצגת: כשהמצגת משתנה, משנים את `deck.json`), וכל 14 המערכות מופיעות (לכל שקף `"act": 1-14`; `script.md` מקבל כותרת `## מערכה N` לכל מערכה, והרינדור מזהיר על מערכה חסרה). **לשון הפנייה ב-`notes`:** תמיד רבים (הדיבור פונה לחדר), בלי קשר ל-`config.voice.address`; רק הטקסט שעל השקף עוקב אחרי `config.voice.address` (הכלל המלא ב-`deck-structure.md` §4). בדיקה מכנית אחרי הרינדור, על שורות התסריט בלי הכותרות: Mac / Git Bash: `grep -v '^###' script.md | grep -nwE "שלך|לך|אותך|איתך|אליך|אצלך|בעצמך|את/ה|תוכל/י"` · Windows PowerShell: `Get-Content script.md | Where-Object { $_ -notmatch '^###' } | Select-String -Pattern '\b(שלך|לך|אותך|איתך|אליך|אצלך|בעצמך|את/ה|תוכל/י)\b'`. כל תוצאה שאינה ציטוט של שיחה (הסוכן ללקוח/ה) = מתקנים ב-`deck.json` ומרנדרים שוב. אין דמו חי; צילומי מסך.
3. מרנדרים: `python3 <skill-dir>/scripts/render_pages.py --config outputs/webinars/<slug>/config.json --only deck` → `deck.html` + `script.md` (התסריט עם ספירת מילים ושניות לכל שקף, 2.5 מילים לשנייה). כותבים את `notes` מראש ליעד 6,700-7,500 מילים סך הכל (45-50 דקות ב-2.5 מילים לשנייה): כ-200 מילים לשקף רגיל, 60-80 לשקף חוצץ. מרנדרים **פעם אחת**. סטייה של יותר מ-15% מהיעד? תיקון אחד ממוקד ורינדור שני, לא יותר. **אפס אזהרות אינטראקציה:** הרינדור מדפיס `! אין אינטראקציה בשקפים N-M` על כל רצף של יותר מ-7 שקפים בלי `[עצירה]`/`[צ'אט]` (וגם על 5 השקפים האחרונים); מוסיפים סימון ב-`notes` של שקף בתוך הטווח באותו תיקון. הסימון נכתב בתוך `notes` בדיוק במשפט שבו פונים לקהל ("[צ'אט] כתבו לי..."); בקשה מהקהל לכתוב בצ'אט בלי סימון = פגם. הרינדור מסיים בשורת סיכום `N אזהרות` ומחזיר קוד יציאה 1 כל עוד יש אזהרת אינטראקציה או מערכה; `ok` על הקבצים לבדו אינו "עבר". **אסור להדפיס את שורת ה-✅ כל עוד המספר בשורת הסיכום גדול מ-0**; מתקנים את `deck.json` ומרנדרים שוב עד `0 אזהרות`.
4. פותחים את `deck.html` למשתמש/ת (`open` / `start`). מסבירים בשורה: חצים לדפדוף, `F` למסך מלא. **PDF ב-2 צעדים, כותבים אותם מילה במילה:** 1) לוחצים על המקש `P` בתוך המצגת (המסך עובר למצב הדפסה: כל השקפים ברצף; `Esc` מחזיר למצגת). 2) Ctrl/Cmd+P ← "שמירה כ-PDF". לא מציעים להקליד `?print` בשורת הכתובת: אחרי דפדוף הכתובת מסתיימת ב-`#s=N`, ומה שמוסיפים אחריו לא מופעל.
5. **מתווה:** במקום 2-4 כותבים `deck-outline.md` (שקף-שקף, עם "מה אומרים" + ספירת מילים) ו-`script.md`.

`✅ שלב 2 הושלם: N שקפים, ~M דקות דיבור · 0 אזהרות רינדור (מועתק משורת הסיכום של הרינדור) · קבצים: deck.json, deck.html, script.md · PDF: המקש P בתוך המצגת, ואז Ctrl/Cmd+P ← שמירה כ-PDF. ממשיכים לשלב 3?`

---

## שלב 3 · דף נחיתה

קוראים `reference/copy-blocks.md`.

1. **לכידת לידים, שאלה אחת, חובה לשאול (AskUserQuestion, לא מדלגים ולא מניחים):** "איפה לשמור את הנרשמים? (א) גיליון Google דרך Google Form מאחורי הטופס המעוצב (מומלץ, 3 דקות, `manual-paths.md` §1), או (ב) בלי טופס: כפתור אחד שמוביל לקבוצת הוואטסאפ." **לפני הבחירה אומרים במפורש** מה (ב) מוותר עליו: לא נאספת אף כתובת מייל, ולכן אין תזכורות במייל (בשלב 6 נכתבות רק 4 הודעות הוואטסאפ) ודף התודה לא חלק מהמשפך (הכפתור מוביל ישירות לקבוצה; הקבוצה היא הרשימה). **מסלול מייל (`next_step: email` / `calendar`): (ב) לא קיים**, כי אין קבוצה שהכפתור יוביל אליה; שואלים רק כדי לאשר (א). (א) = נותנים את ההוראות ומבקשים `form_action` + מזהי `entry.`; אפשר לדחות ולרנדר בינתיים עם (ב), אבל רק אחרי שהמשתמש/ת בחר/ה במפורש; לעולם לא נופלים למצב (ב) בשקט. המצב נקבע רק לפי `form_action` בקונפיג (יש = טופס, אין = כפתור); בשורת ה-✅ מדווחים את המצב בדיוק כפי שהסקריפט הדפיס בשורת `registration:`, לא לפי מה שנבחר בשיחה. **מיד אחרי שנקבע מצב כפתור:** המצגת נכתבה בשלב 2 לפני שהמצב היה ידוע, לכן בודקים את ה-`notes`: `grep -n -E 'מייל|דף התודה|נרשמת' deck.json` (Mac / Git Bash) או `Select-String -Path deck.json -Pattern 'מייל|דף התודה|נרשמת'` (Windows PowerShell). כל שורה שמתארת איך הנרשמים/ות קיבלו את לינק הקבוצה נכתבת מחדש בנוסח ניטרלי (`reference/deck-structure.md` §6), ומרנדרים שוב `--only deck`; זה הרינדור השני היחיד המותר למצגת.
2. כותבים `copy.json`: 8 סקשנים + `thank_you`, בקול מ-`config.voice`, בלי המילים האסורות, בלי מספרים בלי מקור.
3. **עצירת אישור מס' 1.** מציגים בצ'אט: כותרת (3 חלופות, המומלצת מסומנת), תת-כותרת, הכפתור, 4 פריטי "מה תקבלו". מתקנים עד אישור; המילה הקנונית: "מאושר". לא מרנדרים לפני.
4. מרנדרים: `python3 <skill-dir>/scripts/render_pages.py --config outputs/webinars/<slug>/config.json --only pages` → `landing/index.html` (+ דף התודה, שנבדק בשלב 4). פותחים למשתמש/ת. בודקים: אפס `{{` בקובץ, כותרת עד 3 שורות בנייד.
5. שאלת ההסכמה ב-Google Form: הדף שולח לשדה ה-`consent` את טקסט האפשרות המדויק מ-`manual-paths.md` §1 (שלב 1). טקסט שונה בטופס, או שאלת הסכמה "חובה" בלי `entry` בקונפיג = Google דוחה את כל ההרשמות בשקט (הדף מציג "תודה" בכל מקרה). לכן: או שמעתיקים את הטקסט אות-באות ונותנים לו `entry`, או שמגדירים את השאלה כלא-חובה.
6. אין `privacy_url`? מזכירים בשורה אחת: "תיבת ההסכמה בלי לינק. לפני שהדף עולה לאוויר צריך דף מדיניות פרטיות; מוסיפים לקונפיג ומרנדרים שוב."

`✅ שלב 3 הושלם: דף הרשמה ב-8 סקשנים, מצב <טופס+גיליון / כפתור לקבוצה> · קבצים: copy.json, landing/index.html. ממשיכים לשלב 4?`

---

## שלב 4 · דף תודה

`reference/copy-blocks.md` §11.

0. מצב כפתור (`form_action` ריק)? דף התודה נוצר ונבדק כרגיל, אבל אומרים בשורה שהוא לא חלק מהמשפך (הכפתור בדף הנחיתה מוביל ישירות לקבוצה) ולא מבטיחים "אחרי ההרשמה תגיע/י לדף תודה".
1. `thank_you` ב-`copy.json` כבר נכתב בשלב 3: "נשאר צעד אחד" (הקבוצה), 3 צעדים, התאריך, ספירה לאחור, שיתוף בוואטסאפ. מציגים את הטקסט בקצרה; תיקון אם צריך.
2. אין `group_link` עדיין (מגיע בשלב 5)? הכפתור מוסתר אוטומטית; אחרי שלב 5 מרנדרים שוב. אומרים את זה.
   - **מסלול מייל:** `group_link` נשאר ריק בכוונה, הכפתור מוסתר לתמיד, ו-`thank_you` ב-`copy.json` נכתב בלי "נשאר צעד אחד" ובלי הקבוצה: "נרשמת. הלינק לשידור והתזכורות יגיעו למייל", התאריך, ספירה לאחור, שיתוף בוואטסאפ. ב-`calendar` מוסיפים שורה "הוסף/הוסיפי ליומן" עם התאריך והשעה (טקסט, בלי קובץ).
3. מרנדרים (`--only pages`), פותחים `landing/thank-you/index.html`.
4. עוברים על הצ'קליסט של `copy-blocks.md` §14 ומדווחים מה עבר ומה נשאר למשתמש/ת (למשל: שליחת טופס בדיקה אחרי ההעלאה). העלאה לאוויר: `manual-paths.md` §4 (Netlify Drop, דקה, גוררים את תיקיית `landing`). אפשר לדחות לסוף.

`✅ שלב 4 הושלם: דף תודה + צ'קליסט · קבצים: landing/thank-you/index.html. ממשיכים לשלב 5?`

---

## שלב 5 · קבוצת וואטסאפ

`reference/manual-paths.md` §2.

0. **מסלול מייל (`next_step: email` / `calendar`): מדלגים.** לא פותחים קבוצה ולא מבקשים לינק. `whatsapp-group.md` מכיל שורה אחת: "אין קבוצת וואטסאפ במסלול מייל; התזכורות נשלחות במייל (`emails.md`)". `state.json` מסמן את שלב 5 כהושלם וממשיכים.
1. כותבים `whatsapp-group.md`: שם לקבוצה, הגדרות קבוצה שקטה (רק מנהלים שולחים), הוראות של 5 צעדים לפתיחה ולהעתקת לינק ההזמנה, כלל "קבוצה 2 מ-800 חברים" (המגבלה: 1,024), והודעת הפתיחה המוצמדת (עד 5 שורות, בקול) + תיאור הקבוצה (2 שורות).
2. מבקשים: "פתח/י את הקבוצה לפי הקובץ והדבק/י כאן את לינק ההזמנה." המשתמש/ת מדביק/ה → `config.group_link` → מרנדרים שוב (`--only pages`) כדי שדף התודה והכפתור יצביעו לקבוצה. **אפשר לדחות** ("נמשיך, נחזור לזה בסוף"): ממשיכים לשלב 6, ושם הלינק נכתב כ-`{{GROUP_LINK}}` (כלל אחד, מפורט בשלב 6) ומוחלף בשלב 7 יחד עם `{{BROADCAST_LINK}}`. במצב כפתור (ב) דחייה = הדף לא עולה לאוויר עד שהלינק בפנים; אומרים את זה במפורש.

כשלינק ההזמנה מתקבל (עכשיו או בשלב 7): כותבים ל-`config.group_link` ומרנדרים שוב (`--only pages`) כדי שכפתור הקבוצה בדף הנחיתה ובדף התודה יעבוד. עד אז הכפתור ריק, ואומרים את זה במפורש: "הדף לא עולה לאוויר לפני שהלינק בפנים."

מסלול וואטסאפ: `✅ שלב 5 הושלם: קבוצה + הודעת פתיחה · קבצים: whatsapp-group.md (+ config.group_link, או "נדחה לשלב 7"). ממשיכים לשלב 6?`
מסלול מייל: `✅ שלב 5 הושלם: דילוג (מסלול מייל, אין קבוצה) · קבצים: whatsapp-group.md (הערה). ממשיכים לשלב 6?`

---

## שלב 6 · תזכורות במייל ובוואטסאפ

קוראים `reference/schedule.md`.

0. **מסתעפים לפי מצב הלכידה** (`form_action` בקונפיג). **מצב כפתור (ריק):** אין רשימת מייל, ולכן אין מיילים. `emails.md` מכיל שורה אחת בלבד, מילה במילה, בלי כותרת ובלי פסקת הסבר: `אין רשימת מייל במצב כפתור; התזכורות הן 4 הודעות הוואטסאפ (ראו `whatsapp.md`)`. כל מילה נוספת בקובץ = כישלון הבדיקה. `schedule.md` בלי שורות מייל ובלי סעיף "מייל" ב"איך שולחים", והתזכורות ליומן מפנות רק ל-`whatsapp.md`. מדלגים על כל מה שמדבר על מיילים בסעיפים 1-3. **מצב טופס:** ממשיכים כרגיל. **מסלול מייל (`next_step: email` / `calendar`, תמיד מצב טופס):** ההפך מהמצב הקודם: 3 המיילים כרגיל, ובלי הודעות וואטסאפ. `whatsapp.md` מכיל שורה אחת בלבד: "אין קבוצת וואטסאפ במסלול מייל; התזכורות הן 3 המיילים" (+ הפניה ל-`emails.md`). הודעת "התחלנו" הופכת למייל רביעי קצר (20-40 מילים, בשעת השידור, עם לינק השידור). `schedule.md` בלי שורות וואטסאפ. מדלגים על כל מה שמדבר על וואטסאפ בסעיפים 1-3.
1. כותבים 3 מיילים (ברוכים הבאים מיד / "מחר ב-T" יום לפני 10:00 / "עוד שעה") ו-4 הודעות וואטסאפ (מחר 10:00 / היום 10:00 / עוד שעה / "התחלנו"). **כלל הלינקים:** `group_link` קיים בקונפיג → כותבים את הלינק עצמו, לא placeholder; רק `broadcast_link` מותר כ-`{{BROADCAST_LINK}}` עד שלב 7. לינק השידור עוד לא קיים? כותבים `{{BROADCAST_LINK}}` ומחליפים בשלב 7. אין עדיין `group_link` כי הקבוצה נדחתה בשלב 5? מציעים פעם אחת: "יש כבר לינק לקבוצה? אם לא, ממשיכים עם `{{GROUP_LINK}}`". אין = כותבים `{{GROUP_LINK}}` (אותו כלל כמו לינק השידור) ומחליפים בשלב 7. אלה שני ה-placeholders היחידים המותרים; שום לינק אחר לא נכתב כ-`{{`.
   - **שבת ושישי:** יום-לפני חל בשבת? שולחים ביום שישי עד 13:00 או במוצאי שבת אחרי 20:30. חל ביום שישי? עד 13:00. אותו כלל לכל שליחה שנופלת בשבת.
   - **לשון הפנייה:** `config.voice.address` (יחיד/רבים) חלה על כל 4 הודעות הוואטסאפ ועל 3 המיילים, בלי ערבוב בין הודעה להודעה.
2. **עצירת אישור מס' 2.** לפני ההצגה בודקים: ספירת מילים לכל מייל (ברוכים הבאים ו"מחר ב-T": 80-160; "עוד שעה": 40-80) ושמייל "ברוכים הבאים" מכיל את 4 כותרות `gains.items` מ-`copy.json`. **שער לשון הפנייה (מכני, לא מהעין):** מריצים על 7 הטקסטים את בדיקת ה-grep מ-`reference/schedule.md` §4 לפי `config.voice.address`; כל שורה שחוזרת = כותבים את ההודעה מחדש לפני ההצגה. מציגים בצ'אט את 3 המיילים (נושא + גוף) ו-4 ההודעות. מתקנים עד אישור; המילה הקנונית: "מאושר".
3. כותבים `emails.md`, `whatsapp.md` (כל טקסט בבלוק קוד, מוכן להעתקה) ו-`schedule.md` (טבלה עם תאריך ושעה אמיתיים לכל שליחה + 4 תזכורות ליומן). מסבירים בשורה איך שולחים: מערכת דיוור (רשימה + תזמון) או Gmail ב-BCC; וואטסאפ מהטלפון לפי הלוח. בדיקה לפני הסיום: אפס `{{` ב-`emails.md`, ב-`whatsapp.md` וב-`schedule.md` מלבד `BROADCAST_LINK` (ו-`GROUP_LINK`, רק אם הקבוצה נדחתה); כל placeholder שהלינק שלו כבר בקונפיג = כישלון הבדיקה.

מצב טופס: `✅ שלב 6 הושלם: 3 מיילים + 4 הודעות + לוח שילוח · אפס {{ ב-emails.md מלבד BROADCAST_LINK · קבצים: emails.md, whatsapp.md, schedule.md. ממשיכים לשלב 7?`
מצב כפתור: `✅ שלב 6 הושלם: 4 הודעות וואטסאפ + לוח שילוח (בלי מיילים, אין רשימה) · קבצים: emails.md (הערה), whatsapp.md, schedule.md. ממשיכים לשלב 7?`
מסלול מייל: `✅ שלב 6 הושלם: 3 מיילים + מייל "התחלנו" + לוח שילוח (בלי וואטסאפ, אין קבוצה) · קבצים: emails.md, whatsapp.md (הערה), schedule.md. ממשיכים לשלב 7?`
נדחתה הקבוצה? מוסיפים לשורה: `· {{GROUP_LINK}} נשאר עד שלב 7`.
   בדיקת פלט במצב כפתור (לפני שורת ה-✅): `grep -n מייל schedule.md` (Mac / Git Bash) או `Select-String -Path schedule.md -Pattern 'מייל'` (Windows PowerShell) חייב להחזיר שורה אחת בלבד: הערת הפתיחה בראש הקובץ (הלוח המצומצם ב-`reference/schedule.md` §0). כל שורה נוספת (שורת מייל בטבלה או סעיף "מייל" ב"איך שולחים") = מוחקים ומריצים שוב.

---

## שלב 7 · הוובינר

קוראים `reference/webinar-day.md`.

1. כותבים `webinar-day.md`: הוראות הקמת **וובינר** בזום (לא פגישה; הקלטה לענן; בלי חדר המתנה; practice session; ומה לעשות אם אין תוסף וובינרים), הודעת "התחלנו" עם הלינק, וצ'קליסט 10 דקות לפני.
2. מבקשים: "צור/י את הוובינר בזום לפי הקובץ והדבק/י את הלינק." → `config.broadcast_link` → מחליפים `{{BROADCAST_LINK}}` ב-`emails.md` (רק במצב טופס), `whatsapp.md`, `schedule.md`. **לא נוגעים ב-`webinar-day.md`:** שם `{{BROADCAST_LINK}}` הוא שם ה-placeholder בתוך הוראה, לא לינק להחלפה. הקבוצה נדחתה בשלב 5? מבקשים גם את לינק ההזמנה עכשיו → `config.group_link` → מחליפים `{{GROUP_LINK}}` באותם 3 קבצים ומרנדרים שוב (`--only pages`). מוודאים אפס `{{` ב-`emails.md`, `whatsapp.md`, `schedule.md`. אפשר לדחות; מה שנדחה מופיע ב"מה נשאר לך לעשות". במסלול מייל הודעת "התחלנו" ב-`webinar-day.md` היא מייל, לא הודעת וואטסאפ.
3. כותבים `after.md`: טבלה למילוי (נרשמו / הצטרפו לקבוצה / הגיעו / נשארו / אחוזים, מאיפה לוקחים כל מספר) + 3 שורות "מה לשפר".
4. בדיקת פלט לפני הסיום: בכל שורה ב-`webinar-day.md` וב-`after.md`, כולל פריטי הרשימה הממוספרת של זום, מילה לועזית לא פותחת שורה (כותבים "נכנסים ל-zoom.us", "נושא (Topic):"), והפנייה דו-מגדרית ("ראה/י", "אני מעדכן/ת").

`✅ שלב 7 הושלם: זום + צ'קליסט + טופס "אחרי" · קבצים: webinar-day.md, after.md.`

---

## הפלט הסופי

אחרי שלב 7 מדפיסים את עץ התיקייה ואת הטבלה, ומסיימים בשורת "מה נשאר לך לעשות" (רק מה שנדחה: לינק קבוצה, לינק זום, העלאה לאוויר, מדיניות פרטיות).

```
outputs/webinars/<slug>/
  config.json · copy.json · deck.json · state.json
  research.md
  deck.html · script.md            (או deck-outline.md)
  landing/index.html · landing/thank-you/index.html
  whatsapp-group.md
  emails.md · whatsapp.md · schedule.md
  webinar-day.md · after.md
```

| # | שלב | ✅ | תוצר |
|---|---|---|---|
| 1 | מחקר | ✅ | `research.md`, `config.json` |
| 2 | מצגת ותסריט | ✅ | `deck.html`, `script.md` |
| 3 | דף נחיתה | ✅ | `landing/index.html` |
| 4 | דף תודה | ✅ | `landing/thank-you/index.html` |
| 5 | קבוצת וואטסאפ | ✅ | `whatsapp-group.md` (במסלול מייל: הערה בלבד) |
| 6 | תזכורות במייל ובוואטסאפ | ✅ | `emails.md` (במצב כפתור: הערה בלבד), `whatsapp.md` (במסלול מייל: הערה בלבד), `schedule.md` |
| 7 | הוובינר | ✅ | `webinar-day.md`, `after.md` |

ושורה אחת בסוף, לא יותר: "רוצה מיתוג משלך, קריאייטיבים לרשתות, או שהמיילים יישלחו לבד? זה במסלול המתקדם: `reference/advanced.md`."

## כללי ברזל

- 7 שלבים, סדר קבוע, שמות קבועים, "ממשיכים?" אחרי כל אחד.
- לא ממציאים נתונים. לא מנסחים מדיניות פרטיות. לא מוחקים את תיבת ההסכמה.
- לא שואלים על עיצוב, חיבורים או מפתחות בבסיס.
- כפתור אחד בדף, נוסח אחד. טלפון חובה בטופס.
- משנים JSON ומרנדרים; לא עורכים HTML ביד.
- שני placeholders בלבד: `{{BROADCAST_LINK}}` ו-`{{GROUP_LINK}}` (רק אם הקבוצה נדחתה), שניהם מוחלפים בשלב 7 ב-`emails.md`, `whatsapp.md`, `schedule.md` בלבד.
- קבצי התוצר שכותבים למשתמש/ת (`*.md` בתיקיית הוובינר) לא מזכירים אף פעם `reference/*.md`, `SKILL.md` או תיקיית הסקיל; מי שקורא/ת אותם לא מכיר/ה את הסקיל מבפנים. הפניות רק לקבצים בתיקיית הוובינר.
- `config.json` מכיל רק את המפתחות של `config.template.json`, בלי מפתחות מומצאים (למשל "מצב" של הלכידה: הוא נגזר מ-`form_action` בלבד ולא נשמר כמפתח).
- נתקע? `reference/troubleshoot.md` לפני שמנסים דברים.
